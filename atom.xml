<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
    <id>https://dellevin.github.io</id>
    <title>Del Levin‘s blog</title>
    <updated>2024-02-22T05:55:33.566Z</updated>
    <generator>https://github.com/jpmonette/feed</generator>
    <link rel="alternate" href="https://dellevin.github.io"/>
    <link rel="self" href="https://dellevin.github.io/atom.xml"/>
    <subtitle>望山叹高，登愈艰。入世哀难，伤无泪。
</subtitle>
    <logo>https://dellevin.github.io/images/avatar.png</logo>
    <icon>https://dellevin.github.io/favicon.ico</icon>
    <rights>All rights reserved 2024, Del Levin‘s blog</rights>
    <entry>
        <title type="html"><![CDATA[将同一层级下面的国标行业分类转换成父包含子的形式]]></title>
        <id>https://dellevin.github.io/post/391TeUBSj/</id>
        <link href="https://dellevin.github.io/post/391TeUBSj/">
        </link>
        <updated>2024-02-22T05:43:40.000Z</updated>
        <content type="html"><![CDATA[<p>这是一开始的形式（部分）</p>
<pre><code class="language-json">[
  {
    &quot;id&quot;: &quot;A&quot;,
    &quot;name&quot;: &quot;农、林、牧、渔业&quot;,
    &quot;pid&quot;: null,
    &quot;level&quot;: &quot;0&quot;,
    &quot;desc&quot;: &quot;本门类包括 01～05 大类&quot;
  },
  {
    &quot;id&quot;: &quot;A01&quot;,
    &quot;name&quot;: &quot;农业&quot;,
    &quot;pid&quot;: &quot;A&quot;,
    &quot;level&quot;: &quot;1&quot;,
    &quot;desc&quot;: &quot;指对各种农作物的种植&quot;
  },
  {
    &quot;id&quot;: &quot;A011&quot;,
    &quot;name&quot;: &quot;谷物种植&quot;,
    &quot;pid&quot;: &quot;A01&quot;,
    &quot;level&quot;: &quot;2&quot;,
    &quot;desc&quot;: &quot;指以收获籽实为主的农作物的种植，包括稻 谷、小麦、玉米等农作物的种植和作为饲料和工业原料的谷物的种植&quot;
  },
  {
    &quot;id&quot;: &quot;A0111&quot;,
    &quot;name&quot;: &quot;稻谷种植&quot;,
    &quot;pid&quot;: &quot;A011&quot;,
    &quot;level&quot;: &quot;3&quot;,
    &quot;desc&quot;: null
  },
  {
    &quot;id&quot;: &quot;A0112&quot;,
    &quot;name&quot;: &quot;小麦种植&quot;,
    &quot;pid&quot;: &quot;A011&quot;,
    &quot;level&quot;: &quot;3&quot;,
    &quot;desc&quot;: null
  },
  {
    &quot;id&quot;: &quot;A0113&quot;,
    &quot;name&quot;: &quot;玉米种植&quot;,
    &quot;pid&quot;: &quot;A011&quot;,
    &quot;level&quot;: &quot;3&quot;,
    &quot;desc&quot;: null
  },
  {
    &quot;id&quot;: &quot;A0119&quot;,
    &quot;name&quot;: &quot;其他谷物种植&quot;,
    &quot;pid&quot;: &quot;A011&quot;,
    &quot;level&quot;: &quot;3&quot;,
    &quot;desc&quot;: null
  },
  {
    &quot;id&quot;: &quot;A012&quot;,
    &quot;name&quot;: &quot;豆类、油料和薯类种植&quot;,
    &quot;pid&quot;: &quot;A01&quot;,
    &quot;level&quot;: &quot;2&quot;,
    &quot;desc&quot;: null
  }
]
</code></pre>
<p>然后将他们转换成这种形式</p>
<pre><code class="language-json">[
    {
        &quot;id&quot;: &quot;A&quot;,
        &quot;name&quot;: &quot;农、林、牧、渔业&quot;,
        &quot;pid&quot;: null,
        &quot;level&quot;: &quot;0&quot;,
        &quot;desc&quot;: &quot;本门类包括 01～05 大类&quot;,
        &quot;children&quot;: [
            {
                &quot;id&quot;: &quot;A01&quot;,
                &quot;name&quot;: &quot;农业&quot;,
                &quot;pid&quot;: &quot;A&quot;,
                &quot;level&quot;: &quot;1&quot;,
                &quot;desc&quot;: &quot;指对各种农作物的种植&quot;,
                &quot;children&quot;: [
                    {
                        &quot;id&quot;: &quot;A011&quot;,
                        &quot;name&quot;: &quot;谷物种植&quot;,
                        &quot;pid&quot;: &quot;A01&quot;,
                        &quot;level&quot;: &quot;2&quot;,
                        &quot;desc&quot;: &quot;指以收获籽实为主的农作物的种植，包括稻 谷、小麦、玉米等农作物的种植和作为饲料和工业原料的谷物的种植&quot;,
                        &quot;children&quot;: [
                            {
                                &quot;id&quot;: &quot;A0111&quot;,
                                &quot;name&quot;: &quot;稻谷种植&quot;,
                                &quot;pid&quot;: &quot;A011&quot;,
                                &quot;level&quot;: &quot;3&quot;,
                                &quot;desc&quot;: null
                            },
                            {
                                &quot;id&quot;: &quot;A0112&quot;,
                                &quot;name&quot;: &quot;小麦种植&quot;,
                                &quot;pid&quot;: &quot;A011&quot;,
                                &quot;level&quot;: &quot;3&quot;,
                                &quot;desc&quot;: null
                            }
</code></pre>
<p>下面是python代码</p>
<pre><code class="language-python"># 划分同一个数据下面的各种类别
import json

# 读取文件并解析 JSON 数据
def read_json_file(file_path):
    with open(file_path, 'r', encoding='utf-8') as file:
        data = json.load(file)
    return data

# 写入到 JSON 文件
def write_json_file(data, file_path):
    with open(file_path, 'w', encoding='utf-8') as file:
        json.dump(data, file, ensure_ascii=False, indent=4)

def build_hierarchy(categories):
    # 创建一个空的字典列表，用于存储最终结果
    result = []

    # 创建字典，用于将分类按照 id 分组
    category_map = {}
    for category in categories:
        category_map[category['id']] = category

    # 遍历分类列表，构建分类树
    for category in categories:
        # 如果当前分类的 pid 为空，则将其视为一级分类
        if category['pid'] is None:
            result.append(category)
        # 否则，将当前分类添加到其父分类的 children 列表中
        else:
            parent_id = category['pid']
            parent_category = category_map.get(parent_id)
            if parent_category is not None:
                if 'children' not in parent_category:
                    parent_category['children'] = []
                parent_category['children'].append(category)

    return result




# 读取数据
categories = read_json_file('D:\ALL_Proj\PyCharm_Proj\pythonProjectDemo01\无标题.json')
# 构建分类树
result = build_hierarchy(categories)
# 写入数据
write_json_file(result, 'D:\ALL_Proj\PyCharm_Proj\pythonProjectDemo01\Aresult.json')

print(&quot;文件写入成功&quot;)

</code></pre>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[爱一个人真的好难]]></title>
        <id>https://dellevin.github.io/post/MiyC8lPTV/</id>
        <link href="https://dellevin.github.io/post/MiyC8lPTV/">
        </link>
        <updated>2024-02-19T09:08:44.000Z</updated>
        <content type="html"><![CDATA[<p>当我经历了一份全身心都投入的失败感情后，突然间发现，我好像失去了爱一个人的能力。或许是哀莫大于心死，我生活的一切都伴随着我的心变得枯燥乏味。我像一个机器人一样的活着，每天执行着三点一线的指令。胖了，丑了，似乎活着的意义只剩下了让自己开心 。以前我是为她而活，昂扬向上，努力刻苦。现在为自己而活，我好像好像失去了动力一样。<br>
我不断的努力去尝试爱一个人，喜欢一个人，可是到头来却发现，我喜欢的只是她们的外表。从未触及她们的灵魂，我是卑鄙的，挺喜欢的名义坠入声色犬马的世界。<br>
好想爱一个人，去灵魂的触碰，去心与心的相拥，想去试试那种奋不顾身的感觉，粉身碎骨也无所谓了。。。可是，我又怎么该劝自己呢？</p>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[前端实现div的高度拖拽改变]]></title>
        <id>https://dellevin.github.io/post/NHn1fzEt2/</id>
        <link href="https://dellevin.github.io/post/NHn1fzEt2/">
        </link>
        <updated>2023-11-06T00:35:42.000Z</updated>
        <content type="html"><![CDATA[<p>实现代码</p>
<h2 id="html">html</h2>
<pre><code class="language-vue">        &lt;div class=&quot;contactPerson_bottom_div&quot; id=&quot;fuDiv&quot;&gt;
            &lt;div class=&quot;dragDiv&quot; @mousedown=&quot;dragEagle&quot; &gt;
                    &lt;el-icon&gt;&lt;SemiSelect /&gt;&lt;/el-icon&gt;
            &lt;/div&gt;
         &lt;/div&gt;

</code></pre>
<h2 id="script">script</h2>
<pre><code class="language-javascript">dragEagle(e) {
            var targetDiv = document.getElementById('fuDiv');
            //得到点击时该地图容器的宽高：
            var targetDivHeight = targetDiv.offsetHeight;
            var startX = e.clientX;
            var startY = e.clientY;
            document.onmousemove = function (e) {
                e.preventDefault();
                //得到鼠标拖动的宽高距离：取绝对值
                var distY = Math.abs(e.clientY - startY);

                //往上方拖动：
                if (e.clientY &lt; startY) {
                    targetDiv.style.height = (targetDivHeight + distY) + 'px';
                }

                //往下方拖动：
                if (e.clientX &lt; startX &amp;&amp; e.clientY &gt; startY) {
                    targetDiv.style.height = targetDivHeight - distY + 'px';
                }

                if (parseInt(targetDiv.style.height) &gt;= 800) {
                    targetDiv.style.height = 800 + 'px';
                }
                if (parseInt(targetDiv.style.height) &lt;= 320) {
                    targetDiv.style.height = 320 + 'px';
                }
            }
            document.onmouseup = function () {
                document.onmousemove = null;
            }
        },
</code></pre>
<h2 id="css">css</h2>
<pre><code class="language-css">.contactPerson_bottom_div {
    position: absolute;
    bottom: 0;
    left: 0;
    right: 0;
    border-top-style: solid;
    border-color: black;
    background: rgb(255, 255, 255);
    color: rgb(0, 0, 0);
    z-index: 99;
    height: 320px;
    overflow-y: auto;/* 允许垂直滚动 */
}
.dragDiv{
 	height:10px;
    display: flex; 
    align-items: center; 
    justify-content: center;   
}
.dragDiv:hover {
    /* background-color: #666; */
    cursor: pointer;/* 在hover的时候，鼠标指针变成手 */
}
</code></pre>
<p>首先针对需要设置的div添加一个id，再通过getElementById获取到目标div。</p>
<p>然后在内部添加一个div，可以设置成自己想要的形式，做明显的标记使用。针对这个div设置操作活动@mousedown。也就是当鼠标按下去的时候调用此dragEagle方法，在此之前一定要传入对象参数e。</p>
<p><code>var targetDivHeight = targetDiv.offsetHeight;</code>: 这行代码获取了目标 <code>div</code> 元素的当前高度，并将其存储在 <code>targetDivHeight</code> 变量中。</p>
<p><code>var startX = e.clientX;</code> 和 <code>var startY = e.clientY;</code>: 这两行代码分别记录了鼠标点击位置的横坐标（X坐标）和纵坐标（Y坐标），用于后续计算鼠标拖动的距离。</p>
<p><code>document.onmousemove = function (e) { ... }</code>: 这是一个鼠标移动事件处理程序，当用户按住鼠标左键并移动时，会触发这个事件处理程序。在这个处理程序中，计算了鼠标拖动的距离，并根据拖动的方向来改变目标 <code>div</code> 元素的高度。</p>
<ul>
<li><code>e.preventDefault();</code>: 这一行代码用于防止浏览器默认的拖拽行为，确保我们自己的逻辑生效。</li>
<li>通过比较鼠标当前的 <code>e.clientY</code> 和初始点击时的 <code>startY</code> 来计算垂直方向上的拖动距离 <code>distY</code>。</li>
<li>接下来的条件判断分别处理向上拖动和向下拖动的情况。如果鼠标向上拖动，就增加目标 <code>div</code> 的高度，如果鼠标向下拖动，就减小目标 <code>div</code> 的高度。这样可以实现调整高度的效果。</li>
<li>最后的两个条件判断用于限制目标 <code>div</code> 的最小和最大高度，以避免高度超出一定范围</li>
</ul>
<p><code>document.onmouseup = function () { ... }</code>: 这是鼠标释放事件处理程序，在鼠标左键释放时触发。它用于清除鼠标移动事件处理程序，即当鼠标释放后停止拖动。</p>
<p>在这之中</p>
<pre><code class="language-js">                if (parseInt(targetDiv.style.height) &gt;= 800) {
                    targetDiv.style.height = 800 + 'px';
                }
                if (parseInt(targetDiv.style.height) &lt;= 320) {
                    targetDiv.style.height = 320 + 'px';
                }
</code></pre>
<p>这一段是限制div拖动的最大高度和最小高度</p>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[mysql的连接函数]]></title>
        <id>https://dellevin.github.io/post/E-wnfsfmK/</id>
        <link href="https://dellevin.github.io/post/E-wnfsfmK/">
        </link>
        <updated>2023-11-03T14:08:33.000Z</updated>
        <content type="html"><![CDATA[<p>今天遇到了一个简单的场景，需要sql连接两个字段显示。</p>
<p>语法是这样的</p>
<pre><code class="language-mysql">SELECT CONCAT_WS(' ',snh.userneeds, snh.beizhu) AS connect_info  ,snh.createTime, lmu.createTime as regTime
FROM  `supplier_needs_history` as snh
LEFT JOIN member_user as mu
ON snh.userId = mu.id
LEFT JOIN login_member_user as lmu
on mu.companyCode  = lmu.companyCode
WHERE mu.companyName ='诚润建工集团有限公司'
ORDER BY snh.createTime DESC
LIMIT 3;
</code></pre>
<p>一开始用的是CONCA函数，但是当时出现了以下的情况</p>
<figure data-type="image" tabindex="1"><img src="https://dellevin.github.io/post-images/1699020545116.png" alt="" loading="lazy"></figure>
<p>也就是说，当我连接的两个字段里面其中一个为null的时候，拼接后的结果也是null</p>
<p>在查询之后使用了CONCAT_WS函数</p>
<p>这是结果</p>
<figure data-type="image" tabindex="2"><img src="https://dellevin.github.io/post-images/1699020551528.png" alt="" loading="lazy"></figure>
<ol>
<li>
<p><code>CONCAT()</code>: <code>CONCAT()</code> 函数用于拼接两个或多个字符串。</p>
<pre><code class="language-sql">SELECT CONCAT(first_name, ' ', last_name) AS full_name FROM employees;
</code></pre>
</li>
<li>
<p><code>CONCAT_WS()</code>: <code>CONCAT_WS()</code> 函数用于拼接字符串，并在每个字符串之间插入指定的分隔符。</p>
<pre><code class="language-sql">SELECT CONCAT_WS(', ', last_name, first_name) AS full_name FROM employees;
</code></pre>
</li>
<li>
<p><code>CONCAT_NULL_YIELDS_NULL</code>: 这是 MySQL 的系统变量，用于控制拼接操作的行为。如果设置为 <code>ON</code>（默认值），当任何一个操作数为 <code>NULL</code> 时，拼接结果将为 <code>NULL</code>。如果设置为 <code>OFF</code>，则不会将 <code>NULL</code> 视为 <code>NULL</code>。</p>
</li>
<li>
<p>字符串运算符 (<code>||</code>): MySQL 支持标准 SQL 中的字符串拼接运算符 <code>||</code>。</p>
<pre><code class="language-sql">SELECT first_name || ' ' || last_name AS full_name FROM employees;
</code></pre>
</li>
</ol>
<p>这些函数和运算符允许您在 SQL 查询中执行字符串拼接操作。您可以根据自己的需求选择适当的函数或运算符。</p>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[WPS-excel 向下选中指定行数并填充根据公式计算好的数据]]></title>
        <id>https://dellevin.github.io/post/UHgvDonW5/</id>
        <link href="https://dellevin.github.io/post/UHgvDonW5/">
        </link>
        <updated>2023-10-22T07:52:25.000Z</updated>
        <content type="html"><![CDATA[<p>工作中用到了数据库和excel相结合的用法，来针对我这种mysql和excle都半斤八两的人。在mysql中针对数据字段的拼接，用的还不是很熟悉，所以结合了一下excel，但是数据量又很大，在网上找了好多办法，终于找到一个相对适应于自己的使用场景的。</p>
<p>先从MySQL中搜索到对应的数据条目</p>
<pre><code class="language-mysql">SELECT 
snh.id,
mu.companyName AS '企业名',mu.supplierId AS '供应商id'
,snh.userneeds AS '用户需求' , snh.refuse AS '拒绝原因' ,snh.beizhu AS '备注', snh.createTime AS '创建时间'
,au.`name` AS '创建者'
FROM `表名` as snh 
LEFT JOIN member_user AS mu ON snh.userId = mu.id
LEFT JOIN admin_user AS au on snh.createUser = au.id
ORDER BY snh.createTime DESC
</code></pre>
<p>然后用navicate导出excel表</p>
<p>导出之后新建字段，拼入自己的excle的函数</p>
<pre><code class="language-bash">=CONCAT(&quot;用户需求:&quot;, IF(D2&lt;&gt;&quot;&quot;, D2, &quot;无&quot;), &quot; - 拒绝原因:&quot;, IF(E2&lt;&gt;&quot;&quot;, E2, &quot;无&quot;), &quot; - 备注:&quot;, IF(F2&lt;&gt;&quot;&quot;, F2, &quot;无&quot;))
</code></pre>
<p><strong>在左上角选择自己需要到的行数</strong></p>
<figure data-type="image" tabindex="1"><img src="https://dellevin.github.io/post-images/1697961221169.png" alt="" loading="lazy"></figure>
<p><strong>选中之后，按住ctrl+enter（回车）</strong></p>
<p><strong>按住ctrl+D 即可填充好计算的结果</strong></p>
<p>然后就可以填充完毕了，当然在小数据量的时候直接下拉就行，这种方式适合数据量相对大一丢i都的。</p>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[FRP内网穿透的使用]]></title>
        <id>https://dellevin.github.io/post/PmMq0JyMx/</id>
        <link href="https://dellevin.github.io/post/PmMq0JyMx/">
        </link>
        <updated>2023-07-30T12:57:00.000Z</updated>
        <content type="html"><![CDATA[<p>今天还好，算是吧frp搞明白了些许，有些时候，一些知识搞不懂， 也许睡醒了就能搞明白了</p>
<p>现在简单记录一下frp的简单使用</p>
<h2 id="frp是什么">frp是什么</h2>
<p>简单地说，frp就是一个反向代理软件，它体积轻量但功能很强大，可以<strong>使处于内网或防火墙后的设备对外界提供服务</strong>，它支持HTTP、TCP、UDP等众多协议。我们今天仅讨论TCP和UDP相关的内容。</p>
<p>项目地址：https://github.com/fatedier/frp</p>
<p>其中frps是服务端的配置，frpc是客户端的配置，每个都有三个文件，服务端只保留服务端的三个，客户端只保留客户端的三个即可。</p>
<p><strong>Windows客户端启动方式</strong>，打开当前文件夹，输入以下命令</p>
<pre><code class="language-bash">frpc -c ./frpc.ini
</code></pre>
<p><strong>Linux服务端启动方式</strong>，打开当前文件夹，输入以下命令</p>
<pre><code class="language-bash">./frps -c ./frps.ini
</code></pre>
<h2 id="通过ip穿透">通过ip穿透</h2>
<p>这种情况只需要拥有一台服务器就可以了。</p>
<h3 id="服务端配置">服务端配置</h3>
<pre><code class="language-bash">[common]
bind_port = 6001
dashboard_port = 6002
token = 12345678
dashboard_user = admin
dashboard_pwd = admin
vhost_http_port = 6003
</code></pre>
<p>现在解释一下服务端配置的一些</p>
<ol>
<li><code>bind_port</code>: FRP 服务端或客户端绑定的端口号。服务端用于接收来自客户端的请求，客户端用于连接服务端。（可自定义）</li>
<li><code>dashboard_port</code>: FRP 的仪表盘（Dashboard）端口号。通过这个端口，您可以使用 Web 界面来查看 FRP 的状态和运行情况。（可自定义）</li>
<li><code>token</code>: FRP 鉴权的 Token。Token 是客户端连接到服务端时进行验证的凭据，用于确保只有具有正确 Token 的客户端可以连接到服务端。（可自定义）</li>
<li><code>dashboard_user</code>: FRP 仪表盘v的用户名。（可自定义）</li>
<li><code>dashboard_pwd</code>: FRP 仪表盘的密码。（可自定义）</li>
<li><code>vhost_http_port</code>: 虚拟主机 HTTP 端口。如果您启用了虚拟主机功能，这个端口将用于 HTTP 请求。（可自定义）</li>
</ol>
<p>当然这里我之打开了http端口，如果你还想的话可以打开https，socket，tcp等各种链接的端口，这些都是可以自定义的</p>
<h3 id="客户端配置">客户端配置</h3>
<pre><code class="language-bash">[common]
server_addr = 101.42.5.22
server_port = 6001
token = 12345678

[web] 
type = http
local_ip = 127.0.0.1
local_port = 4000
remote_port = 6003
custom_domains = 101.42.5.22
</code></pre>
<ol>
<li><code>[common]</code> 部分：
<ul>
<li><code>server_addr</code>: FRP 服务端的 IP 地址或域名。</li>
<li><code>server_port</code>: FRP 服务端的端口号。</li>
<li><code>token</code>: FRP 鉴权的 Token，与服务端配置中的 Token 要一致。</li>
</ul>
</li>
<li><code>[web]</code> 部分：
<ul>
<li><code>type</code>: 代理类型，这里是 http 类型，用于配置 HTTP 代理。</li>
<li><code>local_ip</code>: 要代理的本地 IP 地址，这里设置为 <code>127.0.0.1</code> 表示代理本地。</li>
<li><code>local_port</code>: 要代理的本地端口号，这里设置为 <code>4000</code>。</li>
<li><code>remote_port</code>: 服务端映射的端口号，客户端请求该端口时将被代理到本地的 <code>local_port</code> 上。</li>
<li><code>custom_domains</code>: 自定义域名，这里设置为 <code>101.42.5.22</code>。</li>
</ul>
</li>
</ol>
<p>其中<code>[web]</code>可以自定义但是要注意名称不能重复</p>
<h2 id="通过域名穿透">通过域名穿透</h2>
<p>这种情况需要配置好域名，服务器的解析，做好解析后才能开始配置。一些服务器提供厂商的还需要再控制台释放对应端口（比如腾讯云，阿里云）。</p>
<h3 id="服务端配置-2">服务端配置</h3>
<pre><code class="language-bash">[common]
bind_port = 6001
dashboard_port = 6002
token = 12345678
dashboard_user = admin
dashboard_pwd = admin
vhost_http_port = 6003
subdomain_host = twoitmen.club
</code></pre>
<p>相同配置不做赘述，这里多出来一个<code>subdomain_host</code>，在这里<code>subdomain_host</code>的作用是自定义子域名的主机名。比如我的服务器绑定的域名是twoitmen.club（我解析的时候喜欢用*做解析，一次配置，随便使用）。那我我在这里填写twoitmen.club就可以了。</p>
<h3 id="客户端配置-2">客户端配置</h3>
<pre><code class="language-bash">[common]
server_addr = 101.42.5.22
server_port = 6001
token = 12345678

[web] 
type = http
local_ip = 127.0.0.1
local_port = 4000
remote_port = 6003
subdomain = frp
</code></pre>
<p>相同配置不做赘述，这里多出来一个<code>subdomain</code>，在这里<code>subdomain</code>的作用是子域名，用于标识代理服务。也就是说，我要访问frp.twoitmen.club:6003就是我穿透完毕后的域名地址。</p>
<h3 id="nginx配置">nginx配置</h3>
<p>在这里多出来一个nginx配置，它是用来反向代理的，就是省略后面端口号的书写</p>
<h4 id="通过域名穿透的方式的nginx配置">通过域名穿透的方式的nginx配置</h4>
<pre><code class="language-bat">
#user  nobody;
worker_processes  1;
events {
    worker_connections  1024;
}
http {
    include       mime.types;
    default_type  application/octet-stream;
    sendfile        on;
    include /home/conf/*.conf;
    keepalive_timeout  65;
    server {
        listen       80;
        server_name  localhost;
        location / {
            root   html;
            index  index.html index.htm;
        }
        
	  location /memos {
		proxy_pass  http://101.42.5.22:6060; # 转发规则
	  }
      
        error_page   500 502 503 504  /50x.html;
        location = /50x.html {
            root   html;
        }
    }
    server {
        listen       80;
        server_name  frp.twoitmen.club;
        	location / {
			proxy_pass http://frp.twoitmen.club:6003/;
			proxy_set_header Host $host;
			proxy_set_header X-Real-IP $remote_addr;
		}
     }
}

</code></pre>
<p>在这里只是多出来一个server，这里简单讲述一下多出来的server配置</p>
<ol>
<li><code>listen 80;</code>: 监听来自端口 80 的 HTTP 请求。</li>
<li><code>server_name frp.twoitmen.club;</code>: 指定该服务器块的域名为 <code>frp.twoitmen.club</code>。</li>
<li><code>location / { ... }</code>: 这是一个代理规则，它会将来自 <code>/</code> 路径的请求转发到 <code>http://frp.twoitmen.club:6003/</code> 上。</li>
<li><code>proxy_pass http://frp.twoitmen.club:6003/;</code>: 它指定了要转发到的目标地址为 <code>http://frp.twoitmen.club:6003/</code>。</li>
<li><code>proxy_set_header Host $host;</code>: 在转发请求时，设置了 <code>Host</code> 请求头为原始请求的主机名。</li>
<li><code>proxy_set_header X-Real-IP $remote_addr;</code>: 在转发请求时，设置了 <code>X-Real-IP</code> 请求头为原始请求的客户端 IP 地址。</li>
</ol>
<h2 id="将frps命令写入system">将frps命令写入system</h2>
<p>输入命令</p>
<pre><code class="language-bash">vim /lib/systemd/system/frps.service
</code></pre>
<p>写入配置</p>
<pre><code class="language-bash">[Unit]
Description=frps service
After=network.target syslog.target
Wants=network.target

[Service]
Type=simple
# 启动服务的命令（此处写你的frps的实际安装目录）
# ExecStart=/your/path/frps -c /your/path/frps.ini
ExecStart=/home/zhang/frpServer/frps -c /home/zhang/frpServerfrps.ini

[Install]
WantedBy=multi-user.target
</code></pre>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[vue3无关系组件之间的方法调用]]></title>
        <id>https://dellevin.github.io/post/MvSGJIhCQ/</id>
        <link href="https://dellevin.github.io/post/MvSGJIhCQ/">
        </link>
        <updated>2023-07-30T12:08:32.000Z</updated>
        <content type="html"><![CDATA[<p>要实现在第一个页面中的方法 <code>onClick</code> 调用第二个页面中的 <code>updateBankToUser</code> 方法，我们可以使用事件总线来实现跨组件通信。在Vue 3中，我们可以使用 <code>mitt</code> 库来创建事件总线。</p>
<p>首先，安装 <code>mitt</code> 库：</p>
<pre><code class="language-bash">npm install mitt
</code></pre>
<p>然后，我们可以创建一个全局的事件总线，并在第二个页面中将 <code>updateBankToUser</code> 方法注册为一个事件处理程序，然后在第一个页面的 <code>onClick</code> 方法中触发这个事件，从而调用第二个页面中的方法。</p>
<p>以下是示例代码：</p>
<p><strong>创建全局事件总线 bus.ts：</strong></p>
<pre><code class="language-ts">// bus.ts
import mitt from 'mitt';

export const bus = mitt();
</code></pre>
<p><strong>第二个页面 user.vue：</strong></p>
<pre><code class="language-vue">&lt;template&gt;
  &lt;div&gt;
    &lt;!-- ...其他代码... --&gt;
    &lt;el-button @click=&quot;updateBankToUser&quot;&gt;更新银行的用户&lt;/el-button&gt;
  &lt;/div&gt;
&lt;/template&gt;

&lt;script setup&gt;
import { bus } from './bus';

const updateBankToUser = async () =&gt; {
  console.log('更新银行的用户');
  // 在这里执行您的业务逻辑
}
bus.on('callUpdateBankToUser', updateBankToUser);
&lt;/script&gt;
</code></pre>
<p><strong>第一个页面 parent.vue：</strong></p>
<pre><code class="language-vue">&lt;template&gt;
  &lt;div&gt;
    &lt;el-button @click=&quot;onClick&quot;&gt;点击调用第二个页面的方法&lt;/el-button&gt;
  &lt;/div&gt;
&lt;/template&gt;

&lt;script setup&gt;
import { bus } from './bus';

const onClick = () =&gt; {
  console.log('调用第二个页面的方法');
  // 在这里执行您的业务逻辑
  bus.emit('callUpdateBankToUser');
}
&lt;/script&gt;
</code></pre>
<p>在这个示例中，我们在 bus.ts 中创建了一个全局事件总线 <code>bus</code>。然后在第二个页面 user.vue 中，我们将 <code>updateBankToUser</code> 方法注册为名为 'callUpdateBankToUser' 的事件处理程序。在第一个页面 parent.vue 中的 <code>onClick</code> 方法中，我们使用 <code>bus.emit</code> 触发了 'callUpdateBankToUser' 事件，从而调用了第二个页面中的 <code>updateBankToUser</code> 方法。</p>
<p>通过这种方式，您可以在不直接引用第二个页面的情况下，实现跨组件调用其中的方法。</p>
<h2 id="什么是全局事件总线busts-import-mitt-from-mitt-export-const-bus-mitt-两行的作用是啥">什么是全局事件总线bus.ts import mitt from 'mitt'; export const bus = mitt(); 两行的作用是啥</h2>
<p>在这里，<code>bus.ts</code> 文件中的两行代码用来创建全局事件总线。全局事件总线是一个简单的事件管理器，它可以用于在应用程序中实现组件之间的通信，特别是当组件之间没有直接父子关系时。</p>
<p>具体来说，这两行代码的作用是：</p>
<ol>
<li>
<p><code>import mitt from 'mitt';</code>: 这里我们导入了 <code>mitt</code> 库，它是一个简单、小巧的 JavaScript 事件库。<code>mitt</code> 库允许我们创建事件总线，以便在应用程序的任何地方都可以访问该事件总线。</p>
</li>
<li>
<p><code>export const bus = mitt();</code>: 在这里，我们创建了一个全局的事件总线 <code>bus</code> 并将其导出。通过 <code>mitt()</code> 调用，我们创建了一个新的事件总线实例，可以用来管理事件的注册、触发和取消。</p>
</li>
</ol>
<p>总结起来，<code>bus.ts</code> 文件中的这两行代码的目的是创建一个全局的事件总线，以便在应用程序的任何地方都可以使用它进行组件之间的通信。通过这个全局事件总线，我们可以在不直接引用其他组件的情况下，实现组件之间的方法调用和数据传递。</p>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[负二进制数相加]]></title>
        <id>https://dellevin.github.io/post/O_Dk45tFL/</id>
        <link href="https://dellevin.github.io/post/O_Dk45tFL/">
        </link>
        <updated>2023-07-25T13:25:29.000Z</updated>
        <content type="html"><![CDATA[<p>给出基数为 <strong>-2</strong> 的两个数 <code>arr1</code> 和 <code>arr2</code>，返回两数相加的结果。</p>
<p>数字以 <em>数组形式</em> 给出：数组由若干 0 和 1 组成，按最高有效位到最低有效位的顺序排列。例如，<code>arr = [1,1,0,1]</code> 表示数字 <code>(-2)^3 + (-2)^2 + (-2)^0 = -3</code>。<em>数组形式</em> 中的数字 <code>arr</code> 也同样不含前导零：即 <code>arr == [0]</code> 或 <code>arr[0] == 1</code>。</p>
<p>返回相同表示形式的 <code>arr1</code> 和 <code>arr2</code> 相加的结果。两数的表示形式为：不含前导零、由若干 0 和 1 组成的数组。</p>
<p><strong>示例 1：</strong></p>
<pre><code>输入：arr1 = [1,1,1,1,1], arr2 = [1,0,1]
输出：[1,0,0,0,0]
解释：arr1 表示 11，arr2 表示 5，输出表示 16 。
</code></pre>
<p><strong>示例 2：</strong></p>
<pre><code>输入：arr1 = [0], arr2 = [0]
输出：[0]
</code></pre>
<p><strong>示例 3：</strong></p>
<pre><code>输入：arr1 = [0], arr2 = [1]
输出：[1]
</code></pre>
<p><strong>提示：</strong></p>
<ul>
<li><code>1 &lt;= arr1.length, arr2.length &lt;= 1000</code></li>
<li><code>arr1[i]</code> 和 <code>arr2[i]</code> 都是 <code>0</code> 或 <code>1</code></li>
<li><code>arr1</code> 和 <code>arr2</code> 都没有前导0</li>
</ul>
<h2 id="代码">代码：</h2>
<pre><code class="language-java">class Solution {
    public int[] addNegabinary(int[] arr1, int[] arr2) {
        // 定义规则：0+1=1，进位向前边减一，够减则减，不够减则借位
        int[] outCome = null;
        if (arr1.length &gt; arr2.length) {
            for (int i = 1; i &lt;= arr2.length; i++) {
                arr1[arr1.length - i] += arr2[arr2.length - i];
            }
            outCome = arr1;
        } else {
            for (int i = 1; i &lt;= arr1.length; i++) {
                arr2[arr2.length - i] += arr1[arr1.length - i];
            }
            outCome = arr2;
        }
        // System.out.println(Arrays.toString(outCome));
        int add = 0;
        for (int i = outCome.length - 1; i &gt; 0; i--) {
            if (outCome[i] + add &gt;= 2) {
                add = -1;
                outCome[i] -= 2;
            } else if (outCome[i] + add &lt; 0) {
                outCome[i - 1] += 1;
                outCome[i] = 1;
                add = 0;
            } else {
                outCome[i] += add;
                add = 0;
            }
        }
        outCome[0] += add;
        //System.out.println(Arrays.toString(outCome));
        if (outCome[0] &gt;= 2) {
            outCome[0] -= 2;
            int[] newOutCome = new int[outCome.length + 2];
            newOutCome[0] = 1;
            newOutCome[1] = 1;
            for (int i = 0; i &lt; outCome.length; i++) {
                newOutCome[2 + i] = outCome[i];
            }
            outCome = newOutCome;
        }
        // System.out.println(Arrays.toString(outCome));
        if (outCome[0] == 0) {
            int zlength = 0;
            while (zlength &lt; outCome.length &amp;&amp; outCome[zlength] == 0) {
                zlength++;
            }
            System.out.println(zlength);
            if (zlength == outCome.length) {
                outCome = new int[1];
                outCome[0] = 0;
            } else {
                int[] newOutCome = new int[outCome.length - zlength];
                for (int i = 0; i &lt; newOutCome.length; i++) {
                    newOutCome[i] = outCome[zlength + i];
                }
                outCome = newOutCome;
            }
        }

        return outCome;
    }
}

</code></pre>
<h2 id="错误的办法">错误的办法：</h2>
<p>这里主要错误的原因是因为忽略了数组之间的长度，因为长度是1000也就是最大的数是-2的1000次幂。远远的超出了bigint，int，以及long类型的计算长度。所以下面的办法是妥妥的不对的</p>
<table>
<thead>
<tr>
<th>关键字</th>
<th>所占位数</th>
<th>范围</th>
</tr>
</thead>
<tbody>
<tr>
<td>int</td>
<td>32</td>
<td>−231−231 ~ 2 ^{31} - 1</td>
</tr>
<tr>
<td>long</td>
<td>64</td>
<td>−263−263 ~ 2 ^{63} - 1</td>
</tr>
</tbody>
</table>
<pre><code class="language-java">class Solution {
    public int[] addNegabinary(int[] arr1, int[] arr2) {
        int xx = lowBinaryToInt(arr1) + lowBinaryToInt(arr2);
        String ss= intToLowBinaryString(xx);
        String[] ssArr = ss.split(&quot;&quot;);
        int[] res = new int[ss.length()];
        for (int i = 0; i &lt;= ss.length()-1; i++) {
            res[i] = Integer.parseInt(ssArr[i]);
        }
        return res;
    }
    public static int lowBinaryToInt(int[] arr) {
        double res=0;
        int xx = 0;
        for (int i = arr.length - 1; i&gt;=0; i--) {
            if (arr[i] == 1) {
                res = res+Math.pow(-2, xx);
                xx++;
            }else{
                xx++;
            }
        }
        return (int)res;
    }
    public static String intToLowBinaryString(int N) {
        StringBuilder sb = new StringBuilder();
        while (N != 0) {
            int r = N % -2;
            N = N / -2;
            if (r &lt; 0) {
                r = r + 2;
                N = N+1;
            }
            sb.append(r);
        }
        return sb.length() &gt; 0 ? sb.reverse().toString() : &quot;0&quot;;
    }
}
</code></pre>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[利用windows定时器实现每天数据库的备份]]></title>
        <id>https://dellevin.github.io/post/C7IpjllCC/</id>
        <link href="https://dellevin.github.io/post/C7IpjllCC/">
        </link>
        <updated>2023-07-25T13:21:49.000Z</updated>
        <content type="html"><![CDATA[<p>事情的起因是今早上我更改前端页面的时候忽然发现，前端数据显示全是一系列的乱码什么的，似乎是被人恶意注入了，简直太痛了。自己没有mysql定时备份的习惯，所以只能含泪偷哭，好在我同事有，多亏他救了一命，还好数据还在。但是他每天都手动备份，着实有些麻烦了。小小的偷懒一下，懒惰是人们进步的阶梯。</p>
<p>因为windows有<strong>任务计划程序</strong>的原因，所以我们只需要写好bash脚本就可以。</p>
<h2 id="bash">bash</h2>
<pre><code class="language-bash">:: 配置数据库用户
SET DB_USER=root

:: 配置数据库密码
SET DB_PASSWORD=root123

:: 配置备份的数据库名称
SET DB_NAME=security_enterprise

:: 配置备份的文件路径
SET SAVE_PATH=D:\MySqlBackup

:: 配置mysqldump的路径,有空格的要加上双引号
SET MYSQL_DUMP_PATH=D:\Environment\mysql-5.7.40\bin\mysqldump.exe

:: 开始工作
:: 跳转到工作目录下
%SAVE_PATH:~0,2%
cd %SAVE_PATH%
:: 设置变量：备份文件名
SET BAK_FILE=%SAVE_PATH%\security_enterprise_bak_%date:~0,4%_%date:~5,2%_%date:~8,2%.sql
:: 开始做备份
%MYSQL_DUMP_PATH% -u%DB_USER% -p%DB_PASSWORD% %DB_NAME%&gt;%BAK_FILE%
</code></pre>
<p>在里面很清楚了，在此不做赘述。</p>
<h2 id="windows任务计划程序">windows任务计划程序</h2>
<h4 id="设置名称描述以及安全选项">设置名称描述以及安全选项</h4>
<figure data-type="image" tabindex="1"><img src="https://dellevin.github.io/post-images/1690291369063.png" alt="" loading="lazy"></figure>
<h4 id="设置触发器">设置触发器</h4>
<figure data-type="image" tabindex="2"><img src="https://dellevin.github.io/post-images/1690291385311.png" alt="" loading="lazy"></figure>
<h4 id="设置操作">设置操作</h4>
<figure data-type="image" tabindex="3"><img src="https://dellevin.github.io/post-images/1690291393632.png" alt="" loading="lazy"></figure>
<h4 id="设置一下意外情况">设置一下意外情况</h4>
<p>这里是防止备份失败进行的容错处理，可以做此步骤</p>
<figure data-type="image" tabindex="4"><img src="https://dellevin.github.io/post-images/1690291399659.png" alt="" loading="lazy"></figure>
<p>经过一系列的设置，就会每天在固定时间在我的D:\MySqlBackup目录下面生成一个备份文件，防止出错了。</p>
<figure data-type="image" tabindex="5"><img src="https://dellevin.github.io/post-images/1690291408403.png" alt="" loading="lazy"></figure>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[mysql中like的查询效率]]></title>
        <id>https://dellevin.github.io/post/wZFFfWDOS/</id>
        <link href="https://dellevin.github.io/post/wZFFfWDOS/">
        </link>
        <updated>2023-07-25T13:19:48.000Z</updated>
        <content type="html"><![CDATA[<p>like %索引%和like   索引%的区别</p>
<p>在 MySQL 中，当使用 <code>LIKE</code> 条件进行字符串匹配时，如果在索引列上使用 <code>LIKE %索引%</code> 和 <code>LIKE 索引%</code> 这两种形式，会有不同的查询性能和结果。</p>
<ol>
<li>
<p><code>LIKE %索引%</code>：<br>
这种情况下，<code>%</code> 通配符位于搜索字符串的开头和结尾，表示可以匹配任意字符。例如，假设索引列是 <code>column_name</code>，我们执行以下查询：</p>
<pre><code class="language-sql">SELECT * FROM table_name WHERE column_name LIKE '%索引%';
</code></pre>
<p>这将查找 <code>column_name</code> 列中包含有 <code>索引</code> 子字符串的所有行。</p>
</li>
<li>
<p><code>LIKE 索引%</code>：<br>
这种情况下，<code>%</code> 通配符仅位于搜索字符串的结尾，表示只匹配以指定字符串开头的值。例如，假设索引列仍然是 <code>column_name</code>，我们执行以下查询：</p>
<pre><code class="language-sql">SELECT * FROM table_name WHERE column_name LIKE '索引%';
</code></pre>
<p>这将查找 <code>column_name</code> 列中以 <code>索引</code> 开头的所有行。</p>
</li>
</ol>
<p>区别：</p>
<ul>
<li><code>LIKE %索引%</code> 的查询可能会涉及全表扫描，因为通配符 <code>%</code> 出现在搜索字符串的开头和结尾，导致无法利用索引。</li>
<li><code>LIKE 索引%</code> 的查询可以利用索引，因为通配符 <code>%</code> 仅出现在搜索字符串的结尾，允许数据库引擎使用索引的前缀部分进行匹配，从而提高查询性能。</li>
</ul>
<p>通常情况下，如果可能，应尽量避免使用 <code>LIKE %索引%</code> 这种形式，特别是对于大型数据表，因为它可能导致较慢的查询性能。如果只需要查找以某个字符串开头的值，应该使用 <code>LIKE 索引%</code> 形式，以便利用索引优化查询。</p>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[小白向-用GitHubAction实现阿里云盘自动打卡]]></title>
        <id>https://dellevin.github.io/post/4AeSeXaT0/</id>
        <link href="https://dellevin.github.io/post/4AeSeXaT0/">
        </link>
        <updated>2023-07-25T13:12:49.000Z</updated>
        <content type="html"><![CDATA[<p>开门见山，针对程序员来说，如果每天都手动签到岂不是很费力，昨天和我同学闲聊的时候，发现他在用py实现各种签到的打卡，看的我是狠狠的抽自己脸，怎么能手动打卡呢！！！尊严何在？</p>
<p>因为上个月在吾爱上看见一个<a href="https://www.52pojie.cn/home.php?mod=space&amp;uid=1942406">zhuoxin99</a>大佬发了一个<a href="https://www.52pojie.cn/forum.php?mod=viewthread&amp;tid=1785892&amp;extra=page%3D1%26filter%3Dtypeid%26typeid%3D388">某盘签到</a>的一个java项目，今天正好心血来潮准备改改修修补补的。因为对GitHub的Action没有什么了解，所以有点怵头。修修补补最终是弄好了</p>
<p>项目地址：https://github.com/dellevin/ToDoTaskForGithubAction</p>
<p>下面针对像我这样的技术小白说一下几个技术要点：</p>
<h2 id="创建工作流">创建工作流</h2>
<p>在<code>./github/workflows</code> 的文件夹下面创建yml文件，文件名随便，后缀名一定是yml的格式</p>
<h2 id="编写配置">编写配置</h2>
<p>下面贴一下我的完整配置</p>
<pre><code class="language-yml">name: Java CI
on:
  schedule:
    - cron: &quot;30 22 * * *&quot;  # 北京时间上午06:30
  workflow_dispatch:   # 添加 workflow_dispatch 触发器，用于手动运行
jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v2

    - name: Set up JDK
      uses: actions/setup-java@v1
      with:
        java-version: '11' # 这里可以指定你需要的 Java 版本

    - name: Cache Maven packages
      uses: actions/cache@v2
      with:
        path: ~/.m2
        key: ${{ runner.os }}-m2-${{ hashFiles('**/pom.xml') }}
        restore-keys: ${{ runner.os }}-m2

    - name: Build with Maven
      run: mvn -B package --file pom.xml   # 使用 Maven 进行项目构建，可以根据你的项目需要修改命令

    - name: run Java Application
      run: |
        java -Djava.security.manager -Djava.security.policy==src/main/resources/policy.policy -jar  target/ToDoTask.jar
      # 附带权限加载运行程序
      env:
        REFRESH_TOKEN: ${{ secrets.REFRESH_TOKEN }}


</code></pre>
<p><code>name: Java CI</code>表示我的工作流叫做Java CI</p>
<p>on里面我就不写了，已经打注释了</p>
<p>接下来是重头戏是job里面的东西们</p>
<p><code>runs-on: ubuntu-latest</code>表示我在这个ubuntu的系统上运行的（一个linux系统）</p>
<p><code>build</code>任务首先做checkout代码操作。</p>
<p>接下来是构建步骤，</p>
<p><code>- name</code> 表示我这个步骤叫什么名</p>
<p>下面表示我是java11版本运行的</p>
<pre><code class="language-yml">uses: actions/setup-java@v1
      with:
        java-version: '11' # 这里可以指定你需要的 Java 版本
</code></pre>
<p>这一步表示 缓存Maven本地包,加速构建，避免了每次运行都要下载maven里面引入的包（ps：我是看别人写的copy过来的）</p>
<pre><code class="language-yml">    - name: Cache Maven packages
      uses: actions/cache@v2
      with:
        path: ~/.m2
        key: ${{ runner.os }}-m2-${{ hashFiles('**/pom.xml') }}
        restore-keys: ${{ runner.os }}-m2
</code></pre>
<p>使用Maven进行项目打包构建。</p>
<pre><code class="language-yml">- name: Build with Maven
  run: mvn -B package --file pom.xml   # 使用 Maven 进行项目构建，可以根据你的项目需要修改命令
</code></pre>
<p>最后调用Java命令运行打包生成的JAR文件</p>
<pre><code class="language-yml">- name: run Java Application
  run: |
    java -Djava.security.manager -Djava.security.policy==src/main/resources/policy.policy -jar  target/ToDoTask.jar
  # 附带权限加载运行程序
  env:
    REFRESH_TOKEN: ${{ secrets.REFRESH_TOKEN }}
</code></pre>
<p>其中<code>-Djava.security.manager -Djava.security.policy==src/main/resources/policy.policy</code>的作用是设置权限，让github设置的screct传入进来，也就是传入到ToDoTask类里面的<code>private static final String refresh_token = System.getenv(&quot;REFRESH_TOKEN&quot;);</code>里面，其中<code>System.getenv(&quot;REFRESH_TOKEN&quot;)</code>就是用来获取GitHub设置的screct的，通过yml配置文件里面的，</p>
<pre><code class="language-yml">  env:
    REFRESH_TOKEN: ${{ secrets.REFRESH_TOKEN }}
</code></pre>
<p>进行传入，如果不加入<code>-Djava.security.manager -Djava.security.policy==src/main/resources/policy.policy</code>这句话的话refresh_token 获取的一直是null。。。。。</p>
<p>因为我每次运行都是各种权限错误，运行一次加一次，最后索性在policy文件下面加了一个<code>permission java.security.AllPermission;</code>释放所有权限</p>
<p>报错log（部分）</p>
<pre><code class="language-java">----1
Error: Exception in thread &quot;main&quot; java.lang.IllegalStateException: java.security.AccessControlException: access denied (&quot;java.lang.RuntimePermission&quot; &quot;getProtectionDomain&quot;)
----2
access denied (&quot;java.lang.RuntimePermission&quot; &quot;setFactory&quot;)
----3
access denied (&quot;java.util.PropertyPermission&quot; &quot;java.protocol.handler.pkgs&quot; &quot;read&quot;)
----4
access denied (&quot;java.util.PropertyPermission&quot; &quot;java.protocol.handler.pkgs&quot; &quot;write&quot;)
----5
access denied (&quot;java.lang.RuntimePermission&quot; &quot;getProtectionDomain&quot;)
    
然后好多权限报错问题，运行一次报一次。。。。。。
</code></pre>
<h3 id="yml配置之-项目切忌class方式运行">yml配置之 项目切忌class方式运行</h3>
<p>一开始我是懵的，创建的是springboot的项目，然后就要编写如何运行java文件的东西，一开始我选择的是采用直接编译java文件运行class类的方式。</p>
<p>这种方式是大错特错的，因为是项目的原因，必须要引入这个</p>
<pre><code>name: Build with Maven
run: mvn -B package --file pom.xml 
</code></pre>
<h3 id="pom配置之-一定要加bulid">pom配置之 一定要加bulid</h3>
<p>也就是说一定要加入下面这段，finalName标签是必须要的，因为是打包成一个jar包运行的是一个jar包，你不加finalName人家都不知道运行啥，运行class文件吗，别搞笑了。</p>
<p>另外plugins标签里面也一定要加上，因为该插件提供了打包和运行Spring Boot项目的能力。添加这个插件后可以直接使用mvn spring-boot:run来运行Spring Boot应用，我不加上无法运行jar包。。。。</p>
<pre><code class="language-xml">&lt;build&gt;
    &lt;!--    打包后的文件名--&gt;
    &lt;finalName&gt;ToDoTask&lt;/finalName&gt;
    &lt;plugins&gt;
        &lt;plugin&gt;
            &lt;groupId&gt;org.springframework.boot&lt;/groupId&gt;
            &lt;artifactId&gt;spring-boot-maven-plugin&lt;/artifactId&gt;
        &lt;/plugin&gt;
    &lt;/plugins&gt;
&lt;/build&gt;
</code></pre>
<h3 id="java文件之获取传入进来的token">java文件之获取传入进来的token</h3>
<p>我一直好奇的是<code>REFRESH_TOKEN: ${{ secrets.REFRESH_TOKEN }}</code>这段是如何传入进来的，结果百度无果之后果断问了gpt，他告诉我用<code>System.getenv(&quot;REFRESH_TOKEN&quot;)</code>接收就好（ps：gpt真厉害）。在我运行工作流之后，发现token是null，很明显是没传入进来嘛，然后又去问，他告诉我要设置权限，也就是上面提到的哪些。。。。这样下来，最终是完成了这个</p>
<p>其中</p>
<pre><code class="language-yml">env:
    REFRESH_TOKEN: ${{ secrets.REFRESH_TOKEN }}
</code></pre>
<p>这一段你要放在外面就是全局变量，你要放在里面就是局部变量</p>
<h2 id="注意">注意</h2>
<p>Github Action对于公有仓库是免费的，对于私有仓库用户提供了一定的免费额度：</p>
<ul>
<li>对于运行在Linux上的作业，github提供了2000分钟的免费额度；</li>
<li>Github提供了10G免费的缓存，并且会对7天内未被访问的key进行删除；</li>
<li>以上的使用限额，会在每个月进行重置；</li>
</ul>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[由刷视频从喜羊羊到大黄蜂的思考]]></title>
        <id>https://dellevin.github.io/post/96Q09f_KF/</id>
        <link href="https://dellevin.github.io/post/96Q09f_KF/">
        </link>
        <updated>2023-05-27T10:52:30.000Z</updated>
        <content type="html"><![CDATA[<p>今天周六早上起床，心血来潮开始刷视频，不知不觉时间就像被偷走一样，和游戏类似，两三个小时转瞬即逝。我每次都想着，诶呀一个一分多钟的视频算什么，下一个！于是在百个视频过去后一上午的时间也消失了。</p>
<p>从喜羊羊，到情感生活，到家庭育儿，到程序编码，到机器制作，再到科幻制作，再到大黄蜂。你可能会奇怪，喜羊羊和情感生活有什么关系，家庭育儿和程序编码有什么关系，机器制作和科幻制作又有什么关系。其实这些没有奇怪的地方，喜羊羊里面的喜羊羊美羊羊沸羊羊的关系引出情感，儿童的幼年编程引出了程序编码，机器制作的炫酷效果引出了科幻制作。</p>
<p>一个视频就像细胞上面的无数的受体和另一个细胞上面的配体一样，他们那种概念的联系将一个个视频连锁成宇宙中的群星看似毫无联系，细看却是万般变化的星座。</p>
<p>我在想是什么变成这样的呢？标签？模糊的标签？也许是这样，但这这是一种浅显的理解吧！因为这种标签，把他们分成了不同的种类，划分成不同的视频，然后用一个概念展示给我们看。罗翔老师讲过一个他的一件故事，他在上大学的时候，初入大学很多一个地区的人会互相认识，他去了他们省的聚会，然后他突然发现下次的聚合没有叫他，才知道是一个市区的聚会。具体内容我早已忘记，但是大概意思是这样的。我们每个人都像网络视频那样被打上了标签，我们用这个来区分自己的同类，异类。我们会和同类笑着说“看啊！那个人说话好怪啊！看啊！那个人吃饭的样子好丑”但是也许你在你同类眼里也是个异类，一个嘲笑他人的异类。于是你不断给自己打标签，做完这个做那个，加入这个加那个，最终像一只蝙蝠一样，鸟兽不分。</p>
<p>扯远了。其实这种标签带来的分化自古就有，可以具象的堪称阶级划分，就像盖茨比赚了再多的钱，举办了再多的宴会也融入不进去权贵阶层。可以抽象的理解为人们的闲话，谁家怎么怎么了，那家又如何如何。这种东西就像是无形的线，勒住我们的思想和手指。大数据的判别让我们困于信息茧房，让我们只看到自己想看的（比如追星的人会刷到自己喜欢明星的一切，不喜欢那个明星的则会刷到各种搞怪的视频）。我们的思想逐渐变得狭隘，三分钟的视频让我们的喜欢和伤心都会变得特别廉价。我经常看到的一句话就是“原本很开心的，看到你这里绷不住了”，“挺难受的，你这评论给我笑嘻了”无数的视频过去，让我们的喜欢随便的说出口，让我们的痛苦说来就来。</p>
<p>很难不否认短视频在释放压力方面是对当代打工人的良药。快节奏+短视频带给无数人不一样的生活见地，可是这种方式终归是饮鸩止渴，浅陋而自大的。你再也不会记得白鹿原里的白嘉轩，只会明白他叫大壮，我们再也不会记得南海十三郎里面的江誉镠，也许他的名字会叫作小亮。真不知百年之后人们的思想会是如何。</p>
<p>我相信还是会有下一个鲁迅的，那种《中国人难道失掉自信力了吗》那种俯首甘为孺子牛的鲁迅。用笔尖刺痛每一人的脊梁。也许我们不是愚蠢，只是不想醒罢了。</p>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[关于vue，ts，echart整合显示地图的实现方式]]></title>
        <id>https://dellevin.github.io/post/9ZIEPLTy8/</id>
        <link href="https://dellevin.github.io/post/9ZIEPLTy8/">
        </link>
        <updated>2023-05-24T14:58:10.000Z</updated>
        <content type="html"><![CDATA[<p>这两天需要做一个大屏的东西，看着网上那个地图的效果很酷很炫，所以想整一个这样的东西，话不多说，开始整。因为项目本体是人人开源项目，我是在这个基础上做的一个二次开发吧。后端代码就省略了。其实很简单。在此就不会做太多的赘述了。</p>
<h2 id="后端">后端</h2>
<p>不过有一个点是可以关注的，就是当你的数据库过于庞大的话，我们可以建一个缓存。也就是一下的示例代码。</p>
<p>2023年5月25日：针对这个数据，因为map里面是放到缓存中了，一直没有释放，所以改用redis，又因为这个是二次开发的项目，所以人家对redis进行了一系列的改造，我在自己新建redis配置类的时候一直报错有冲突，所以在看了一下结构之后，对后端的代码进行了一系列的重构。</p>
<h3 id="redis工具类">redis工具类</h3>
<p>用于从 Redis 中获取缓存数据或者将数据存入 Redis</p>
<pre><code class="language-java">
    public List&lt;Object&gt; getListFromRedis(String key) {
        Object cachedData = get(key);
        if (cachedData != null) {
            // 如果缓存中存在数据，则将其转换为 List 对象并返回
            return (List) cachedData;
        } else {
            // 如果缓存中不存在数据，则返回空列表
            return new ArrayList&lt;&gt;();
        }
    }
    public void setListToRedis(String key, List&lt;Object&gt; data, long expire) {
        set(key, data, expire);
    }

</code></pre>
<h3 id="控制层">控制层</h3>
<p>修改 <code>getEnterpriseNums</code> 方法，以先从 Redis 中获取数据，如果缓存中存在数据，则直接返回；如果缓存中不存在数据，则从数据库中获取数据，并将其存入 Redis 缓存中</p>
<pre><code class="language-java">    @Autowired
    private RedisUtils redisUtils;
	@GetMapping(&quot;/enterpriseNums&quot;)
    @ApiOperation(&quot;返回地图上的区域数据&quot;)
    public Result getEnterpriseNums(){
//        List&lt;ShowEnterpriseDetailsNumsDTO&gt; enterpriseDetailsList = enterpriseDetailsService.getEnterpriseNums();
//        return  new Result().ok(enterpriseDetailsList);
        String redisKey = &quot;enterpriseNums&quot;; // 定义 Redis 缓存的键名
        List enterpriseDetailsList = redisUtils.getListFromRedis(redisKey);
        if (enterpriseDetailsList.isEmpty()) {
            // 从数据库中获取数据
            enterpriseDetailsList = enterpriseDetailsService.getEnterpriseNums();
            // 将数据存入 Redis 缓存，设置过期时间为一小时
            redisUtils.setListToRedis(redisKey, enterpriseDetailsList, RedisUtils.HOUR_ONE_EXPIRE);
        }
        return new Result().ok(enterpriseDetailsList);
    }
</code></pre>
<p><s>首先从 <code>showEnterpriseDetailsServicesCache</code> 缓存中获取工厂信息列表数据。如果列表数据为空（即缓存中没有数据），则调用 <code>enterpriseDetailsService.getEnterpriseNums()</code> 方法获取工厂信息数据，并将其存入缓存中。在下次调用 <code>getEnterpriseNums()</code> 方法时，将直接从缓存中获取数据，避免了重复查询的开销。</s></p>
<p>通过以上优化，当第一次请求 <code>getEnterpriseNums</code> 接口时，数据将从数据库中获取，并存入 Redis 缓存中。之后的请求将直接从 Redis 缓存中获取数据，从而提高响应速度和性能。请确保 Redis 配置正确，并已经启动了 Redis 服务。</p>
<h2 id="前端">前端</h2>
<p>echarts示例：https://echarts.apache.org/examples/zh/editor.html?c=map-HK</p>
<p>他给的示例我们可以分析出来，我觉得是通过ajax请求来把地图搞到，然后绑定map最后，把数据显示出来。<code>$.get(ROOT_PATH + '/data/asset/geo/HK.json', function (geoJson)</code>用于异步加载地图数据。这段代码的作用是通过 AJAX 请求获取名为 <code>'HK.json' 的地图数据文件，然后在成功获取数据后，使用</code>echarts.registerMap('HK', geoJson)<code>将地图数据注册到 ECharts 中，并命名为 </code>'HK'<code>。这样，之后在配置地图的 </code>series<code>中就可以通过指定</code>map: 'HK'` 来使用该地图数据。</p>
<p>再者我们需要提前的把地图数据导入到项目当中来。可以通过这个平台下载地图的数据</p>
<p>http://datav.aliyun.com/portal/school/atlas/area_selector#&amp;lat=30.332329214580188&amp;lng=106.72278672066881&amp;zoom=3.5</p>
<figure data-type="image" tabindex="1"><img src="https://dellevin.github.io/post-images/1684940490186.png" alt="" loading="lazy"></figure>
<p>然后的话导入方式，因为项目的原因我们需要这样做，</p>
<p><strong>先是import</strong></p>
<pre><code class="language-typescript">import jiNanMapJson from &quot;src/views/enterprise/assets/json/济南市.json&quot;;

import * as echarts from &quot;echarts&quot;;
</code></pre>
<p><strong>再然后注册地图数据到 ECharts：</strong></p>
<pre><code class="language-typescript">echarts.registerMap('jiNan', jiNanMapJson);
</code></pre>
<p><strong>更新 <code>mapOption</code> 中的 <code>series</code> 配置，将 <code>map: 'HK'</code> 添加到相关的地图系列中，以便使用注册的地图数据：</strong></p>
<pre><code class="language-typescript">const mapOption = ref({
  // ...其他配置项
  series: [
    {
      // ...其他系列配置
      map: 'jiNan', // 使用注册的地图数据
      // ...其他系列配置
    }
  ]
});
</code></pre>
<p>通过这种方式，我们可以注册到其中，完整代码如下：</p>
<pre><code class="language-vue">&lt;template&gt;
  &lt;div class=&quot;box&quot;&gt;
    &lt;el-form-item&gt;
      &lt;el-button type=&quot;warning&quot; @click=&quot;importHandle()&quot;&gt;{{ $t(&quot;excel.import&quot;) }}&lt;/el-button&gt;
    &lt;/el-form-item&gt;
    &lt;div style=&quot;width: 100%;margin-left: 6em;margin-bottom: 2em;&quot;&gt;
      &lt;div class=&quot;topShow&quot;&gt;
        &lt;div style=&quot;color:brown; font-size: 1.875rem /* 30/16 */;text-align: center;padding-top: 1.4375rem /* 23/16 */;&quot;&gt;
          信用金桥大数据屏幕展示平台&lt;/div&gt;
        &lt;div style=&quot;color:brown; font-size: 1.25rem /* 20/16 */;text-align: right;padding-right: 2.1875rem /* 35/16 */;;&quot;&gt;
          {{ dataForm.currentTime }}&lt;/div&gt;
      &lt;/div&gt;
    &lt;/div&gt;


    &lt;div class=&quot;line_01&quot;&gt;
      &lt;figure class=&quot;figure_left&quot;&gt;
        &lt;div v-for=&quot;(item, index) in dataForm.enterpriseNums&quot; :key=&quot;index&quot; style=&quot;width: 23em;text-align: center; &quot;&gt;
          &lt;div class=&quot;card&quot;&gt;
            &lt;span style=&quot;color: red;&quot;&gt;{{ item.name }}区&lt;/span&gt;
            在营企业总数 ：
            &lt;span style=&quot;color: sienna; font-size: 2em;&quot;&gt;{{ item.value }} 家&lt;/span&gt;
          &lt;/div&gt;
          &lt;br /&gt;
        &lt;/div&gt;
      &lt;/figure&gt;

      &lt;figure class=&quot;figure_center&quot;&gt;

      &lt;/figure&gt;
      &lt;figure class=&quot;figure_right&quot;&gt;
        &lt;v-chart :option=&quot;mapOption&quot; :autoresize=&quot;true&quot; /&gt;
      &lt;/figure&gt;
    &lt;/div&gt;
    &lt;div class=&quot;line_02&quot;&gt;
      &lt;figure class=&quot;figure_left&quot;&gt;
        &lt;v-chart :option=&quot;pie&quot; :autoresize=&quot;true&quot; /&gt;
      &lt;/figure&gt;
    &lt;/div&gt;



    &lt;import ref=&quot;importRef&quot;&gt;&lt;/import&gt;
  &lt;/div&gt;
&lt;/template&gt;

&lt;script lang=&quot;ts&quot; setup&gt;
import { ref, provide, reactive } from &quot;vue&quot;;
import { use } from &quot;echarts/core&quot;;
import { CanvasRenderer } from &quot;echarts/renderers&quot;;
import { BarChart, LineChart, PieChart, MapChart, RadarChart, ScatterChart, EffectScatterChart, LinesChart } from &quot;echarts/charts&quot;;
import { GridComponent, PolarComponent, GeoComponent, TooltipComponent, LegendComponent, TitleComponent, VisualMapComponent, DatasetComponent, ToolboxComponent, DataZoomComponent } from &quot;echarts/components&quot;;
import VChart, { THEME_KEY } from &quot;vue-echarts&quot;;
import app from &quot;@/constants/app&quot;;
import { getToken } from &quot;@/utils/cache&quot;;
import Import from '@/views/enterprise/enterprise-library-import.vue';
import baseService from &quot;@/service/baseService&quot;;
import jiNanMapJson from &quot;src/views/enterprise/assets/json/济南市.json&quot;;
import zhangQiuMapJson from &quot;src/views/enterprise/assets/json/章丘区.json&quot;;
import * as echarts from &quot;echarts&quot;;

use([BarChart, LineChart, PieChart, MapChart, RadarChart, ScatterChart, EffectScatterChart, LinesChart, GridComponent, PolarComponent, GeoComponent, TooltipComponent, LegendComponent, TitleComponent, VisualMapComponent, DatasetComponent, CanvasRenderer, ToolboxComponent, DataZoomComponent]);

provide(THEME_KEY, &quot;westeros&quot;);

const dataForm = reactive({
  currentTime: '',
  enterpriseNums: [],
});


const importRef = ref();
const importHandle = () =&gt; {
  importRef.value.init(`${app.api}/enterprise/details/import?token=${getToken()}`);
};

const list = ref([]);
const industryNames = ref([]);
const getIndustryInfo = async () =&gt; {
  let result = await (await baseService.get(&quot;/enterprise/details/industryInfo&quot;)).data;
  list.value = result;
  industryNames.value = result.map((item: any) =&gt; item.name);
};
getIndustryInfo();

const updateTime = () =&gt; {
  const currentDate = new Date();
  const year = String(currentDate.getFullYear());
  const month = String(currentDate.getMonth() + 1).padStart(2, '0');
  const date = String(currentDate.getDate()).padStart(2, '0');
  const hours = String(currentDate.getHours()).padStart(2, '0');
  const minutes = String(currentDate.getMinutes()).padStart(2, '0');
  const seconds = String(currentDate.getSeconds()).padStart(2, '0');
  dataForm.currentTime = `${year}年${month}月${date}日  ${hours}:${minutes}:${seconds}`;
};
setInterval(() =&gt; {
  updateTime();
}, 1000);

const mapMsg = ref([]);
const getEnterpriseNums = async () =&gt; {
  let result = await (await baseService.get(&quot;/enterprise/details/enterpriseNums&quot;)).data;
  dataForm.enterpriseNums = result;
  mapMsg.value = result;
  //console.log(mapMsg)
};
getEnterpriseNums();

echarts.registerMap('jiNan', jiNanMapJson);
const mapOption = ref({
  title: {
    text: '济南市各区在营企业统计',
    subtext: '数据来源XXXX',
  },
  tooltip: {
    trigger: 'item',
    formatter: '{b}&lt;br/&gt;{c} (家)'
  },
  toolbox: {
    show: false,
    orient: 'vertical',
    left: 'right',
    top: 'center',
    feature: {
      dataView: { readOnly: false },
      restore: {},
      saveAsImage: {}
    }
  },
  visualMap: {
    min: 800,
    max: 500000,
    text: ['High', 'Low'],
    realtime: false,
    calculable: true,
    inRange: {
      color: ['lightskyblue', 'yellow', 'orangered']
    }
  },
  series: [
    {
      name: '济南市各区注册企业数量',
      type: 'map',
      map: 'jiNan',
      label: {
        show: true
      },
      data: mapMsg,
    }
  ]
});


const pie = ref({
  title: {
    text: &quot;行业占比&quot;,
    left: &quot;center&quot;
  },
  tooltip: {
    trigger: &quot;item&quot;,
    formatter: &quot;{a} &lt;br/&gt;{b} : {c} ({d}%)&quot;
  },
  legend: {
    type: 'scroll',
    orient: &quot;vertical&quot;,
    right: 10,
    top: 20,
    bottom: 20,
    left: &quot;left&quot;,
    data: industryNames
  },
  series: [
    {
      name: &quot;行业占比&quot;,
      type: &quot;pie&quot;,
      label: {
        show: false
      },
      radius: &quot;55%&quot;,
      center: [&quot;50%&quot;, &quot;60%&quot;],
      data: list,
      emphasis: {
        itemStyle: {
          shadowBlur: 10,
          shadowOffsetX: 0,
          shadowColor: &quot;rgba(0, 0, 0, 0.5)&quot;
        }
      }
    }
  ]
});
&lt;/script&gt;

&lt;style lang=&quot;less&quot; scoped&gt;
.topShow {
  width: 90%;
  height: 5rem
    /* 80/16 */
  ;
  font-family: &quot;得意黑&quot;;
  border-radius: 3.125rem
    /* 50/16 */
  ;
  border-radius: 3.4375rem
    /* 55/16 */
  ;
  border-radius: 2.5rem
    /* 40/16 */
  ;
  background: #f0f0ff;
  box-shadow: .4375rem
    /* 7/16 */
    .4375rem
    /* 7/16 */
    .875rem
    /* 14/16 */
    #ccccd9,
    -.4375rem
    /* 7/16 */
    -.4375rem
    /* 7/16 */
    .875rem
    /* 14/16 */
    #ffffff;
}

@font-face {
  font-family: '得意黑';
  src: url('./assets/font/SmileySans-Oblique-2.ttf');
  font-weight: normal;
  font-style: normal;
}

.card {
  border-radius: .5rem
    /* 8/16 */
  ;
  font-family: &quot;得意黑&quot;;
  font-size: large;
  border-radius: 1.125rem
    /* 18/16 */
  ;
  background: #f0f0ff;
  box-shadow: inset .4375rem
    /* 7/16 */
    .4375rem
    /* 7/16 */
    .875rem
    /* 14/16 */
    #ccccd9,
    inset -.4375rem
    /* 7/16 */
    -.4375rem
    /* 7/16 */
    .875rem
    /* 14/16 */
    #ffffff;
}

.box {
  position: relative;
  display: flex;
  flex-direction: column;
  justify-content: center;

  figure {
    display: inline-block;
    // position: relative;
    //margin: 2em auto;
    border: .0625rem
      /* 1/16 */
      solid rgba(0, 0, 0, 0.1);
    border-radius: .5rem
      /* 8/16 */
    ;
    box-shadow: 0 0 2.8125rem
      /* 45/16 */
      rgba(0, 0, 0, 0.2);
    padding: 1.875rem
      /* 30/16 */
    ;

    .echarts {
      width: 40vw;
      min-width: 25rem
        /* 400/16 */
      ;
      height: 18.75rem
        /* 300/16 */
      ;
    }
  }
}

.line_01 {
  width: 100%;
  height: auto;

  // 左对齐
  .figure_left {
    float: left;
    width: 27em;
    border-radius: 1.125rem
      /* 18/16 */
    ;
    background: #f0f0ff;
    box-shadow: .4375rem
      /* 7/16 */
      .4375rem
      /* 7/16 */
      .875rem
      /* 14/16 */
      #ccccd9,
      -.4375rem
      /* 7/16 */
      -.4375rem
      /* 7/16 */
      .875rem
      /* 14/16 */
      #ffffff;
  }

  //右对齐
  .figure_right {
    float: right;
    width: 50em;

    .echarts {
      width: auto;
      height: 43.75rem
        /* 700/16 */
      ;
    }

    border-radius: 1.125rem
    /* 18/16 */
    ;
    background: #f0f0ff;
    box-shadow: .4375rem
    /* 7/16 */
    .4375rem
    /* 7/16 */
    .875rem
    /* 14/16 */
    #ccccd9,
    -.4375rem
    /* 7/16 */
    -.4375rem
    /* 7/16 */
    .875rem
    /* 14/16 */
    #ffffff;
  }

  .figure_center {
    //float: right;
    //position: absolute;
    width: 21em;
    border-radius: 1.125rem
      /* 18/16 */
    ;
    background: #f0f0ff;
    box-shadow: .4375rem
      /* 7/16 */
      .4375rem
      /* 7/16 */
      .875rem
      /* 14/16 */
      #ccccd9,
      -.4375rem
      /* 7/16 */
      -.4375rem
      /* 7/16 */
      .875rem
      /* 14/16 */
      #ffffff;
  }
}

.line_02 {
  width: 100%;
  height: auto;

  // 左对齐
  .figure_left {
    float: left;
    width: 50em;
    border-radius: 1.125rem
      /* 18/16 */
    ;
    background: #f0f0ff;
    box-shadow: .4375rem
      /* 7/16 */
      .4375rem
      /* 7/16 */
      .875rem
      /* 14/16 */
      #ccccd9,
      -.4375rem
      /* 7/16 */
      -.4375rem
      /* 7/16 */
      .875rem
      /* 14/16 */
      #ffffff;
  }
}&lt;/style&gt;

</code></pre>
<p>最终实现效果可参考echarts的案例。</p>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[前端实现时间刷新]]></title>
        <id>https://dellevin.github.io/post/Q8Hv-3swf/</id>
        <link href="https://dellevin.github.io/post/Q8Hv-3swf/">
        </link>
        <updated>2023-05-24T14:57:02.000Z</updated>
        <content type="html"><![CDATA[<p>前端实现个时间刷新的案例：</p>
<h2 id="标签">标签</h2>
<pre><code class="language-html">&lt;div style=&quot;color:brown; font-size: 1.25rem /* 20/16 */;text-align: right;padding-right: 2.1875rem /* 35/16 */;;&quot;&gt;
          {{ dataForm.currentTime }}&lt;/div&gt;
</code></pre>
<h2 id="script">script</h2>
<pre><code class="language-typescript">const updateTime = () =&gt; {
  const currentDate = new Date();
  const year = String(currentDate.getFullYear());
  const month = String(currentDate.getMonth() + 1).padStart(2, '0');
  const date = String(currentDate.getDate()).padStart(2, '0');
  const hours = String(currentDate.getHours()).padStart(2, '0');
  const minutes = String(currentDate.getMinutes()).padStart(2, '0');
  const seconds = String(currentDate.getSeconds()).padStart(2, '0');
  dataForm.currentTime = `${year}年${month}月${date}日  ${hours}:${minutes}:${seconds}`;
};
</code></pre>
<p>这里主要调用的是 Date();的前端的方法。</p>
<p>然后我们设置定时器一秒刷新一次。</p>
<pre><code class="language-typescript">setInterval(() =&gt; {
  updateTime();
}, 1000);
</code></pre>
<p>这是根据人人开源项目做二次开发写的，其实二次开发真的有些难受。</p>
<p>如果要根据vue的方式来写就是这样的</p>
<pre><code class="language-vue">&lt;script&gt;
export default {
  data() {
    return {
      currentTime: ''
    };
  },
  mounted() {
    // 更新时间
    this.updateTime();

    // 每秒钟更新一次时间
    setInterval(() =&gt; {
      this.updateTime();
    }, 1000);
  },
  methods: {
    updateTime() {
  const currentDate = new Date();
  const year = currentDate.getFullYear();
  const month = String(currentDate.getMonth() + 1).padStart(2, '0');
  const day = String(currentDate.getDate()).padStart(2, '0');
  const hours = String(currentDate.getHours()).padStart(2, '0');
  const minutes = String(currentDate.getMinutes()).padStart(2, '0');
  const seconds = String(currentDate.getSeconds()).padStart(2, '0');
  this.currentTime = `${year}-${month}-${day} ${hours}:${minutes}:${seconds}`;
    }
  }
};
&lt;/script&gt;

</code></pre>
<p>在<code>mounted</code>生命周期钩子中，我们通过调用<code>updateTime</code>方法来初始化时间，并使用<code>setInterval</code>函数每秒钟更新一次时间。</p>
<p><code>updateTime</code>方法会获取当前时间并更新<code>currentTime</code>的值，然后将其显示在页面中。</p>
<p><code>getYear</code>方法返回的是四位数的年份，<code>getMonth</code>方法返回的是从0开始的月份（需要加1），<code>getDate</code>方法返回的是月份中的日期。我们将它们转换为字符串并使用<code>padStart</code>方法进行补零操作，以保证两位数的格式。</p>
<p>最后，我们将年、月、日、时、分和秒拼接为一个完整的日期时间字符串，并将其赋值给<code>currentTime</code>，用于在页面中显示。</p>
<p>其中有个点就是在获取日期时，<code>getDay()</code>方法返回的是星期几（0-6），而不是日期。今天就犯糊涂了，用了<code>getday()</code></p>
<p>另外推荐一个很好的拟态风格的css生成网站，简直不要太好用！！！！</p>
<p>https://neumorphism.io/#e0e0e0</p>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[判断两个事件是否存在冲突]]></title>
        <id>https://dellevin.github.io/post/Xv57DZrGN/</id>
        <link href="https://dellevin.github.io/post/Xv57DZrGN/">
        </link>
        <updated>2023-05-17T14:47:57.000Z</updated>
        <content type="html"><![CDATA[<p>给你两个字符串数组 <code>event1</code> 和 <code>event2</code> ，表示发生在同一天的两个闭区间时间段事件，其中：</p>
<ul>
<li><code>event1 = [startTime1, endTime1]</code></li>
<li><code>event2 = [startTime2, endTime2]</code></li>
</ul>
<p>事件的时间为有效的 24 小时制且按 <code>HH:MM</code> 格式给出。</p>
<p>当两个事件存在某个非空的交集时（即，某些时刻是两个事件都包含的），则认为出现 <strong>冲突</strong> 。</p>
<p>如果两个事件之间存在冲突，返回 <code>true</code> ；否则，返回 <code>false</code> 。</p>
<p><strong>示例 1：</strong></p>
<pre><code>输入：event1 = [&quot;01:15&quot;,&quot;02:00&quot;], event2 = [&quot;02:00&quot;,&quot;03:00&quot;]
输出：true
解释：两个事件在 2:00 出现交集。
</code></pre>
<p><strong>示例 2：</strong></p>
<pre><code>输入：event1 = [&quot;01:00&quot;,&quot;02:00&quot;], event2 = [&quot;01:20&quot;,&quot;03:00&quot;]
输出：true
解释：两个事件的交集从 01:20 开始，到 02:00 结束。
</code></pre>
<p><strong>示例 3：</strong></p>
<pre><code>输入：event1 = [&quot;10:00&quot;,&quot;11:00&quot;], event2 = [&quot;14:00&quot;,&quot;15:00&quot;]
输出：false
解释：两个事件不存在交集。
</code></pre>
<p><strong>提示：</strong></p>
<ul>
<li><code>evnet1.length == event2.length == 2.</code></li>
<li><code>event1[i].length == event2[i].length == 5</code></li>
<li><code>startTime1 &lt;= endTime1</code></li>
<li><code>startTime2 &lt;= endTime2</code></li>
<li>所有事件的时间都按照 <code>HH:MM</code> 格式给出</li>
</ul>
<h2 id="代码">代码</h2>
<pre><code class="language-java">class Solution {
    public boolean haveConflict(String[] event1, String[] event2) {
        int event1StartTime = strToIntTime(event1[0]);
        int event1EndTime   = strToIntTime(event1[1]);
        int event2StartTime = strToIntTime(event2[0]);
        int event2EndTime   = strToIntTime(event2[1]);

        if(event1EndTime &lt; event2StartTime || event2EndTime &lt;event1StartTime ){
            return false;
        }
        return true;
    }
    public static int strToIntTime(String str) {
        String[] ss = str.split(&quot;:&quot;);
        int hours = Integer.parseInt(ss[0])*60;
        return  hours+Integer.parseInt(ss[1]);
    }
}
</code></pre>
<h2 id="思路">思路</h2>
<p>比对时间不在另一个区间内就可以了，但是string不能进行比较，只能使用int类型比较。</p>
]]></content>
    </entry>
</feed>